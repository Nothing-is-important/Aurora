# Aurora 变更日志

## 增值轮 · 托盘常驻与系统通知 ✅（完成）

- **系统托盘**：启动即常驻（图标随安装包分发），点击切换显示/隐藏，右键菜单（显示 / 最小化到托盘 / 退出）
- **完成通知**：窗口隐藏或最小化时，回复完成弹 Windows 系统通知（静默、点击唤回窗口）；前台时静默不打扰
- 冒烟断言：托盘创建成功、Notification.isSupported 记录

## 开源准备轮 · Windows 风格 + 审计补全 + 文档 ✅（完成）

- **Windows 风格窗口按钮**：右上角最小化/最大化/关闭（Win11 悬停样式，关闭红色），标题居左，标题栏 32px；审计改为位置断言（按钮位于 x>1100）
- **修复检查器折叠黑屏**：根因 = `useMemo` 位于条件 return 之后导致 hooks 顺序变化、React 崩溃；全部 hooks 前置，折叠态改窄条（40px），并新增折叠/展开往返冒烟断言防回归
- **修复导出体验**：导出按钮常驻检查器 header（原来藏在信息 tab），无会话时禁用，异常捕获反馈；冒烟新增导出按钮点击 → 反馈提示的 UI 流程断言
- **审计覆盖补全**：折叠/展开、导出按钮点击、模型下拉切换（切换生效 + 切回）全部纳入 12 阶段冒烟
- **网络代理支持**：设置 → 网络代理（HTTP 代理，undici ProxyAgent），应用于模型请求/联网搜索/网页抓取；冒烟验证配置存取链路
- **开源文档**：README（功能全景/安装/上手/架构）、MIT LICENSE、完整使用教程（docs/user-guide.md）、小红书引流文案（docs/xiaohongshu-文案.md）、深浅色截图入仓（docs/screenshots/）
- 最终打包验证：打包版全量冒烟 exit 0

## 迭代 10/10 · 打磨与加固 ✅（完成）

- **错误重试**：错误卡片「重试」按钮（截断错误消息重发前一条用户消息）；修复 onDone 覆盖 error 状态、error 先于 done 到达的时序问题
- **响应超时**：SSE 整体 180 秒超时（独立 AbortController，与手动停止信号组合）
- **数据自动备份**：每日首次启动复制数据库到 `userData/backups`，保留最近 7 份
- **窗口状态记忆**：窗口位置/大小/最大化状态持久化（屏幕边界钳制，防抖保存）
- **性能**：尝试 manualChunks 分包发现 highlight.js 循环依赖 TDZ 崩溃 → 回退单 bundle（本地应用无网络瓶颈，可靠性优先），文档记录原因
- **最终打包**：`Aurora Setup 0.1.0.exe`（含 MCP mock 服务器 extraResource），打包版全量冒烟 exit 0
- 冒烟扩展为 **12 阶段**：新增错误场景端到端（mock 一次性失败 → 错误卡片 → 点击重试 → 成功）

### 迭代 10 审计结果
- 开发版冒烟 3 连跑全绿；打包版全量冒烟 exit 0 ✅

## 迭代 9/10 · 知识库 RAG ✅（阶段三启动）

- **本地知识库**：添加文件夹 → 递归扫描文本/代码文件（<500KB）→ 按段落分块（~1200 字符）→ 独立 kb.sqlite 落库
- **检索**：中文 unigram+bigram 分词 + 英文词元化的 **BM25 排序**（零依赖零下载，文档不出本机），top-k 片段返回
- **search_knowledge 工具**：Agent 可直接检索知识库，结果带结构化引用
- **引用溯源**：`file://` 本地文件引用卡片（点击用系统默认应用打开原文件），检查器「引用」tab 汇总去重
- 设置弹窗知识库管理：添加文件夹/重建索引/移除
- 冒烟真实建库（2 个 md 文件）→ BM25 检索命中断言 → Agent 端到端

### 迭代 9 审计结果
- 2 文件入索引 ✅、检索「知识库 检索」命中 2 条且首条为最相关文档 ✅、5 张工具卡片/5 条引用 ✅

> 备注：向量嵌入（ONNX 本地模型）作为可选增强已列入 roadmap 备胎池，当前 BM25 检索对个人知识库已足够实用。

## 迭代 8/10 · MCP 客户端 ✅（完成）

- **MCP 客户端**（参考开源 DeepSeek Harness 的 MCP Server 能力，[社区版仓库](https://github.com/HenryZ838978/deepseek-harness)）：stdio JSON-RPC 传输——initialize 握手 → tools/list 注册 → tools/call 调用（60s 超时），请求-响应配对与错误处理
- **Agent 循环接入**：MCP 工具以 `mcp__server__tool` 命名并入 tools 定义，模型可像内置工具一样发起调用
- **设置配置区块**：MCP 服务器列表（启用开关/删除）+ 添加表单（名称/命令/参数），保存即重连，启动时从设置恢复
- 内置 mock MCP 服务器（scripts/mock-mcp-server.mjs，打包时进 extraResources）供演示与全链路冒烟
- 修复：工具名解析正则无法匹配带下划线的服务器 id（改用 lastIndexOf 分隔）

### 迭代 8 审计结果
- 真实子进程握手 ✅、工具注册 1 个 ✅、echo 返回 `echo: aurora-mcp-ok` ✅、Agent 端到端（模型发起→MCP 执行→结果回传）✅

## 迭代 7/10 · 联网能力 ✅（完成）

- **web_search**：零配置联网搜索（cn.bing.com 结果页解析，标题/链接/摘要结构化，15s 超时），无需任何 API Key
- **fetch_url**：网页抓取 + 正文提取（HTML→纯文本、2MB 上限、30KB 截断）
- **引用卡片**：工具步骤携带结构化 refs，消息内可点击引用链接（openExternal 走主进程校验）；检查器「引用」tab 按 URL 去重汇总展示标题/链接/摘要
- 冒烟网络使用 SMOKE 模拟数据（稳定不受网络波动影响），另有独立真实网络探测（记录不阻断）

### 迭代 7 审计结果
- 搜索端到端 ✅、3 条引用卡片 + 检查器引用 tab ✅、真实网络探测 Bing 可达 ✅

## 迭代 6/10 · 代码执行与系统命令 ✅（完成）

- **run_python**：工作目录内异步执行 Python 代码（30s 超时、输出 20KB 截断、未安装 Python 的友好报错）
- **run_shell**：系统命令异步执行（同超时/截断），执行前**确认弹窗**（允许一次 / 始终允许 / 拒绝），"始终允许"自动加入白名单
- **命令白名单**：前缀匹配 + `*` 通配；设置弹窗内按行编辑管理；白名单命中免确认
- Mock 增加 shell/python 工具演示场景；冒烟实现真实端到端：`echo aurora-shell-ok` 经确认流程执行并断言输出；**本机 Python 真实执行 `print(6*7)` 断言输出 42**

### 迭代 6 审计结果
- shell 端到端 ✅、白名单匹配单元验证 ✅、Python 真实执行输出 "42" ✅、两张工具步骤卡片 ✅

## 迭代 5/10 · 工具链基础 ✅（阶段二启动）

- **Agent 工具循环**（主进程）：请求携带 OpenAI tools 定义，流式累积 tool_calls → 执行 → 结果以 tool 角色回传 → 多轮直到 stop（上限 6 轮）
- **工具执行器**：`read_file` / `write_file` / `list_dir`，全部限制在**工作目录沙箱**（`userData/workspace`，路径解析防 `../` 越界），读文件 200KB 上限、输出截断
- **步骤可视化**：`chat:tool` 事件推送步骤状态（running/done/error），消息内毛玻璃步骤卡片（图标/名称/参数摘要/状态动画），检查器「工具调用」tab 汇总展示参数与结果
- **持久化**：tool_steps_json 列自动迁移，重启恢复
- Mock 增加工具演示流（写文件 → 第二轮确认），冒烟实现**真实端到端**：请求写 hello.py → 工具真实落盘工作目录 → 内容逐字断言 → 步骤卡片/DB/检查器全链路验证

### 迭代 5 审计结果
- hello.py 真实写入且内容 `print("hello from aurora")` ✅，DB 步骤落盘 ✅，卡片与检查器渲染 ✅

## 迭代 4/10 · 提示词系统 ✅（完成）

- **系统提示词注入**：发送时自动注入，会话级 > 全局优先级；conversations 表新增 system_prompt 列（自动迁移）
- **全局系统提示词**：设置弹窗编辑（等宽字体 textarea + 保存反馈）
- **会话级覆盖**：检查器「信息」tab 编辑当前会话的覆盖提示词
- **模板库**：输入区书签按钮弹出菜单，6 个内置模板（翻译/代码审查/写作润色/周报生成为填入输入框类；头脑风暴/Python 专家为设为系统提示词类，带 toast 反馈）；预留自定义模板存储接口
- 冒烟记录 chat:start 请求载荷，直接断言 system 消息内容（会话级覆盖生效、优先级正确）

### 迭代 4 审计结果
- 注入验证：最后请求 `messages[0] = {role:'system', content:'会话级冒烟覆盖提示词'}` ✅
- 模板菜单：打开/6 条目/点击填入输入框/自动关闭 ✅

## 迭代 3/10 · Token 统计与用量 ✅（完成）

- **计量**：主进程记录首字延迟（first_token_ms）与总耗时（duration_ms），随 done 事件下发
- **持久化**：messages 表新增 usage_json / duration_ms / first_token_ms 列（自动迁移），重启后恢复
- **费用估算**：DeepSeek 官方定价表（chat ¥2/¥8 每百万、reasoner ¥4/¥16），Mock 演示价，兼容端点按未知处理；`≈¥` 自适应精度显示
- **消息 meta 行**：助手消息底部显示 `242 tokens · 首字 753ms · 耗时 1.7s · ≈¥0.0005`（流完成后出现）
- **检查器用量总览**：信息 tab 顶部卡片（Tokens 总量、输入/输出、消息数、费用）

### 迭代 3 审计结果
- usage meta 文本逐项断言、总览 242/24/218/≈¥0.0005、DB usage 与耗时落盘 ✅

## 迭代 2/10 · 对话增强 ✅（完成）

- **消息操作栏**（悬停浮现，毛玻璃胶囊按钮）：
  - 用户消息：编辑（分支）· 复制
  - 助手消息：重新生成 · 复制
- **编辑消息 = 分支**：截断该消息及其后的所有消息（内存 + SQLite `deleteMessagesFrom`），用新文本重发
- **重新生成**：复用前一条用户消息、截断该助手消息，只追加新回复（修复了重复追加用户消息的 bug）
- **复制**：走主进程 clipboard IPC（避开渲染层权限问题），1.5 秒"已复制"反馈
- **导出**：检查器「信息」tab 显示会话详情（消息数/创建与最近活动/置顶）+ 导出 Markdown（含思维链折叠块）与 JSON（完整结构化）；保存对话框 + 冒烟模式直落盘验证
- **修复用户反馈**：助手回复气泡宽度随内容增长的问题 → 固定全宽（气泡宽 = 消息列宽），审计断言防回归

### 迭代 2 审计结果
- 冒烟新增验证：编辑重发（2 条消息/无错误）、重新生成（流启动与完成/无错误）、复制（主进程剪贴板内容逐字比对）、导出 MD/JSON（内容与路径）、DB 分支截断落盘 ✅
- 气泡宽度 661 = 容器 661 ✅

## 迭代 1/10 · 命令面板与快捷键 ✅（完成）

- **命令面板（Ctrl/Cmd+K）**：Spotlight 风格居中弹窗，统一搜索会话/模型/动作；↑↓ 导航、↵ 执行、esc 关闭（含焦点外 Esc 兜底）、鼠标悬停跟随、无结果空态
- 动作：新对话（Ctrl N）、打开设置（Ctrl ,）、切换主题、切换模型、切换会话
- **快捷键体系**：Ctrl+K 面板、Ctrl+N 新对话、Ctrl+, 设置、Ctrl+1~9 切换最近会话、Esc 关闭弹窗（设置面板同步支持）
- **全局唤起**：Ctrl+Shift+A 显示/聚焦窗口，已聚焦则最小化（globalShortcut，退出时注销）
- 参考开源方案：Cherry Studio 可配置快捷键设计（[官方文档](https://docs.cherryai.com.cn/pre-basic/settings/key-shortcut)）
- **修复两个测试竞态**：① 停止计时器相对 send 触发，避免负载抖动打在空流 ② 冒烟验证 effect 重复触发导致通知覆盖（ref 守卫只发一次）——顺带修复了 Ctrl+N 清空消息后误报停止失败的问题

### 迭代 1 审计结果
- 冒烟 5 连跑全绿：面板打开/8 条目/过滤 1 条/Esc 关闭/Ctrl+N 清空/全局快捷键注册 ✅

## 第 4 轮 · 设置中心 + 附件 + 打包 ✅（完成）

- **设置中心弹窗**（苹果风双栏）：模型列表 + 编辑表单（名称/提供方/Base URL/API Key/模型 ID/温度/Max Tokens/Top P/启用开关）
- 支持添加/删除自定义模型、**连接测试**（请求 `/models` 端点，8 秒超时，返回端点模型数）
- API Key 密码框可显示切换，DPAPI 加密存储
- **附件**：文件选择对话框（图片/文本/代码类型识别）、输入区缩略图/文件芯片、删除
- 图片附件以 `image_url` 多模态部分发送（仅 OpenAI 兼容端点，DeepSeek 官方不支持视觉）；文本附件内联进上下文（<200KB）
- 依赖瘦身：渲染层依赖全部走 Vite 打包，运行时仅 sql.js → 减小安装包
- 应用图标：System.Drawing 生成渐变星芒 icon（512px）
- **electron-builder NSIS 打包**：`release\Aurora Setup 0.1.0.exe`（100MB，可选安装目录、桌面/开始菜单快捷方式）
- 打包版冒烟测试通过（exit 0）：asar 内 sql.js wasm 加载正常、三阶段验证全过
- 修复：打包版截图写入 asar 只读路径导致挂起 → 改存 userData；加启动日志 + 110s watchdog 兜底

### 第 4 轮审计结果
- 开发版冒烟：设置弹窗开/关、3 模型、表单字段齐、附件按钮 ✅
- 打包版冒烟：exit 0，DB/流式/停止/会话切换在 asar 环境全部正常 ✅

**🎉 阶段一 MVP 完成**：界面骨架、聊天核心、会话管理、设置、附件、Windows 安装包全部交付。

## 第 3 轮 · 会话管理 ✅（完成）

- **数据层**：会话/消息 CRUD（conversations/messages 表），消息 upsert 自动刷新会话 updated_at，旧库 error 列迁移
- **自动标题**：首条消息前 24 字自动生成会话标题
- **useChat 会话化**：发送时落库（用户+助手行先行写入再启动流）、流式增量防抖持久化、切换会话从 SQLite 重载、事件订阅全局化（切走不丢流）
- **侧边栏**：真实会话列表（置顶优先 + 时间排序）、相对时间显示、双击重命名（Enter 保存/Esc 取消）、悬停置顶/删除、搜索过滤、空状态
- **冒烟第三阶段**：自动创建第二会话 → 切换（验证清空）→ 切回（验证 4 条恢复）→ 主进程直查 SQLite 断言落盘
- **修复关键竞态**：新建会话时 activeId 触发的 load 与 send 的落库赛跑，读到半成品数据导致消息重复——方案：新建会话跳过 load（send 自行填充）+ 加载代际守卫（过期 load 丢弃）

### 第 3 轮审计结果
- 三阶段全绿：停止截断 ✅ / 会话切换恢复 4 条 ✅ / DB 落盘（2 会话 4 消息，含思维链与代码内容）✅
- 侧边栏 2 项、标题栏自动标题、搜索框、零控制台错误 ✅

## 第 2 轮 · 聊天核心 ✅（完成）

- **SQLite 存储层**（sql.js，零原生编译）：settings / models / conversations / messages 四表，防抖持久化到 `%APPDATA%\aurora\aurora.db`
- API Key 用 Windows DPAPI（safeStorage）加密存储
- **主进程聊天服务**：OpenAI 兼容 SSE 流式转发（content / reasoning_content / usage 分事件推送）、AbortController 停止、HTTP 错误透传
- **Mock 提供方**：无需网络与 Key 的本地模拟流（含思维链/代码块/公式），供演示与测试
- 内置三个模型配置：Mock / DeepSeek Chat / DeepSeek Reasoner（Key 在设置中配置）
- **渲染层**：useChat 状态机（流式增量/停止/错误/usage）、模型下拉菜单（配置状态指示灯）、思维链折叠面板（思考动画）、流式光标、停止按钮（红方块）、错误卡片
- **Markdown 全渲染**：react-markdown + GFM + 表格，代码块语法高亮（双主题自定义 hljs 配色）+ 复制按钮，KaTeX 行内/块级公式
- **冒烟审计升级为两阶段**：① 完整流式 Mock 对话（验证思维链/高亮/公式/复制）② 流式中途点击停止（验证截断生效、不进入错误态）
- 修复：sql.js SqlValue 类型兼容、db 类型收窄问题

### 第 2 轮审计结果
- 中途停止：stoppedEarly=true, errored=false ✅
- DOM：思维链/代码块/hljs/KaTeX/复制按钮全部就位，结束后停止按钮消失，零控制台错误 ✅
- SQLite 落盘 36KB ✅

## 第 1 轮 · 项目骨架 ✅（完成）

- 初始化 Electron 43 + React 18 + TypeScript + Vite 6 + Tailwind 3 工程
- Electron 主进程：无边框窗口、交通灯 IPC、nativeTheme 主题桥接
- 预加载脚本：contextBridge 安全桥（窗口/主题）
- React 渲染层：三栏工作台（毛玻璃侧边栏 264 + 对话区 + 检查器 300）
- 苹果风视觉：氛围光斑背景、玻璃材质、交通灯悬停符号、弹簧缓动、Inter 字体
- 深浅色主题：跟随系统 + 手动循环切换（system → light → dark）
- **数值化冒烟审计框架**：像素采样（交通灯颜色/亮度/方差）+ DOM 布局断言，自动截图存 `shots/` 供人工查看
- 环境适配：绕过失效本地代理直连 npm，Electron 二进制走 npmmirror 镜像
- 修复：冒烟模式下主进程主题被渲染层覆盖的问题（getSource 泄漏）

### 第 1 轮审计结果
- 浅色平均亮度 0.91 / 深色 0.11，主题切换生效 ✅
- 交通灯红 146 / 黄 139 / 绿 161 像素命中 ✅
- 布局 264 / 718 / 300，标题栏 44px，无水平溢出，零控制台错误 ✅
